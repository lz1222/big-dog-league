# 迷宫项目进度跟踪

## 当前状态

- 当前阶段：`B2.1-A`
- 当前子阶段：第一弯局部几何、墙端、扰动轨迹和 Round15 Dry Run
- 状态：`IN_PROGRESS`
- 自动运动：`禁止`
- 第一弯执行：`禁止`
- 第二弯及完整迷宫：`禁止`
- 当前 Git 基线：`16251ae`

## 已完成

- B0 点云、Odom、IMU 和 DDS 基础审计。
- B1 前向五扇区感知和静态距离标定。
- B1.5 Move、StopMove、forwarder watchdog、SDK server watchdog、非法报文
  与遥控急停独立测试，历史记录为通过。
- 旧 B2 状态机可推进第一弯到 `route=1/5`。
- Round15 后补充的 `0.413m` 粗扫掠保护可在旧状态机重放约 `4.52deg`、
  `left≈0.362m` 时触发 `turn_sweep_unsafe`。
- B2.1-A 已实现局部占据图、后向三扇区、有限墙线/墙端、拟合置信度、动态
  矩形足迹、连续采样、停止尾程和六类第一左弯候选。
- 候选已使用六级结论和安全优先字典序，JSON 每帧输出排名前 5。
- Round15 回放器按历史点云快照和后续 Odom 实际路径检查连续足迹，并单独
  输出新几何与旧 `0.413m` 门限证据。
- B2.1-A 新增测试与全仓回归共 `100/100` 通过。
- 开发机合成 2424 点/362 占据单元/4 墙段场景：构图中值约 `30ms`，六候选
  含扰动规划约 `0.16s`；该数据不是 Foxy/AArch64 真机验收。
- `B2_1_A_ROUND15_REPLAY` 已使用完整真机 bag 执行只读回放：322 帧局部图、
  3299 条 Odom，录制点云频率 `14.653Hz`，构图中值 `5.608ms`。
- 回放将最早 `left_side` 原始危险点保留为独立事实，并在接触前 `7.278s`
  关联到合格有限短墙片段。短片段只用于证据追溯，不计入转弯墙模型。

## 最近失败

`Round15`：策略推进到 `1/5`，但左转中段约 `44deg` 时左侧机身轻触内侧
挡板。该轮物理结果为 `FAIL`，不计入首弯通过次数。

`B2_1_A_ROUND15_REPLAY`：物理轨迹仍在约 `44deg` 发生左侧机身轻触，
因此物理结果保持 `FAIL`。本次只读重放已在预测转角约 `23.980deg`、接触前
`7.278s` 报告 `left_side`，并关联 `fragment_001` 有限墙片段（长度约
`0.200m`、关联置信度约 `0.500`）。回放门禁为 `DRY_RUN_PASS`，不是首弯
物理通过，也不允许进入 B2.1-B。

## 当前未完成

- 真机后向覆盖率、局部地图频率和地面/自身过滤验证。
- 步态筛选和真实运动模型，属于 B2.1-B，当前不得提前运动。

## 阻塞项

- Round15 回放门禁已通过，但旧物理轨迹的最小预测间隙仍为 `0m`；它只能
  证明规划会拒绝旧路径，不能证明存在可执行安全路径。
- watchdog 和 estop 的只读 Bool 状态 Topic 尚未确认真实发布源。
- 动态足迹、模型扰动和停止尾程尚未按冻结步态标定。

## 下一步唯一推荐任务

在真机静止状态下完成 B2.1-A 的后向三扇区覆盖、局部图频率以及地面/机身
过滤验证，并保存只读录包。验收通过前不得启动运动桥或发送非零命令。

## 阶段结论

- 是否允许进入 B2.1-B：`否`
- 原因：Round15 回放门禁已通过，但后向覆盖、局部图真机验证、动态余量和
  步态运动模型均仍为 `UNVERIFIED`。
