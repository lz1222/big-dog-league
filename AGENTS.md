# 仓库代码生成约定

- 新增或修改代码时，必须为模块用途、关键类和函数、状态机、安全保护及非直观算法添加简洁的中文注释或中文文档字符串。
- 注释应说明设计意图、数据方向、边界条件和安全原因，避免逐行翻译代码。
- 对外协议字段、ROS Topic、状态常量、命令行参数和既有日志文本保持原有命名，避免因注释要求改变接口兼容性。
- 只修改注释时不得顺带改变运行逻辑、参数默认值或硬件控制行为。
